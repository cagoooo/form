rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write for now to support the UI-based admin login
    // In a production environment with Firebase Auth, you would restrict this.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
